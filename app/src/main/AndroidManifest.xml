<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.composeapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposeApp">
        <activity
            android:name=".KeyboardTestActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ComposeLayoutActivity"
            android:exported="true"
            android:label="@string/title_activity_compose_layout"
            android:theme="@style/Theme.ComposeApp.NoActionBar" />
        <activity
            android:name=".ClickedActivity"
            android:exported="true"
            android:label="@string/title_activity_clicked"
            android:theme="@style/Theme.ComposeApp.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ComposeApp.NoActionBar">

        </activity>
    </application>

</manifest>